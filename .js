<!-- Custom JavaScript -->
<script>
    // Function to toggle image size on click
    function toggleImageSize(imageId) {
        var img = document.getElementById(imageId);
        if (img.style.height === "200px") {
            img.style.height = "400px";
        } else {
            img.style.height = "200px";
        }
    }

    // You can add more JavaScript functions or code here
</script>
